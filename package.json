{
  "name": "pli",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.18 <23"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build --mode lecteur && vite build --mode atelier",
    "preview": "vite preview",
    "verifie": "node scripts/verifie.mjs",
    "types": "tsc --noEmit && tsc --noEmit -p tsconfig.isomorphe.json",
    "test": "node --test src/lib/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.9.3",
    "vite": "^8.2.1"
  }
}
