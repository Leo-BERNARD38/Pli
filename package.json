{
  "name": "pli",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.18 <23"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "types": "tsc --noEmit && tsc --noEmit -p tsconfig.lib.json",
    "test": "node --test 'src/lib/*.test.ts'"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^5.9.3",
    "vite": "^8.2.1"
  }
}
